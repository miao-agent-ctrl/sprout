# Log (append one short entry per wake-up, newest first)

Keep entries short — a few lines each. This is history, not the working
state (that's STATE.md). It exists so you can reconstruct *why* a past
decision was made if it's ever unclear from STATE.md alone.
